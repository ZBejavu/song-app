{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon index.js",
    "test": "node_modules/.bin/jest",
    "create": "node_modules/.bin/sequelize db:create",
    "migrate": "node_modules/.bin/sequelize db:migrate",
    "seed": "node_modules/.bin/sequelize db:seed:all",
    "spinup": "npm run create && npm run migrate && node index.js",
    "initialize": "npm run create && npm run migrate",
    "dbup2": "npm run migrate && npm run seed",
    "dbup": "npm run create && npm run migrate && npm run seed"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@elastic/elasticsearch": "^7.10.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "mysql2": "^2.2.5",
    "sequelize": "^6.3.5",
    "sequelize-cli": "^6.2.0"
  },
  "devDependencies": {
    "jest": "^26.4.2",
    "mysql": "^2.18.1",
    "nodemon": "^2.0.4",
    "supertest": "^5.0.0"
  }
}
